import React from 'react';
import { Layout } from './Layout';

export default function IndexPage() {
    return (
        <Layout>
            <p>
                AlterOrb API Viewer
            </p>
        </Layout>
    );
}
